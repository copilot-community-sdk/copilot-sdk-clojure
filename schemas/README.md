# Upstream Copilot CLI JSON Schemas

These files are fetched verbatim from the `github-copilot-1.0.89-3-linux-x64.tgz` GitHub Release asset for Copilot CLI version `1.0.89-3` and verified against the release `SHA256SUMS.txt`.

**Do not edit by hand.** To update, run `bb schemas:fetch` after bumping `.copilot-schema-version`.

Schema version: `1.0.89-3`
